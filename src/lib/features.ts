/**
 * Site-wide feature switches.
 *
 * SHOW_EVENTS hides the events section on the home page and the Events link in
 * the footer. Flip it back to true to restore both; nothing was deleted.
 * The /events page itself still exists and remains reachable by direct link.
 */
export const SHOW_EVENTS = false;
